#GOAL 1:  Create a program that can tell you whether or not you can make a peanut butter and jelly sandwich

# bread = 2
# jelly = 1
# pb = 1
# if (bread == 2 and jelly == 1 and pb == 1):
#	print "You can make a sandwich!"
# elif (bread >= 2 and jelly >= 1 and pb >= 1):
#	print "You can make a sandwich, and then some!"
# else:
#       print "You can't make a sandwich!"

#bread = raw_input('What is your bread? ')
#jelly = raw_input('What is your jelly? ')
#pb = raw_input('What is your pb?' )
#bread = int(bread)
#jelly = int(jelly)
#pb = int(pb)
#if bread == 2 and jelly == 1 and pb == 1:
#       print "You can make a sandwich!"
#elif bread >= 2 and jelly >=1 and pb >=1:
#       print "You can make a sandwich and then some."
#else:
#       print "You can't make a sandwich!"

#bread = int(raw_input('What is your bread? '))
#jelly = int(raw_input('What is your jelly? '))
#pb = int(raw_input('What is your pb?' ))
#if bread == 2 and jelly == 1 and pb == 1:
#        print "You can make a sandwich!"
#elif bread >= 2 and jelly >=1 and pb >=1:
#        print "You can make a sandwich and then some."
#else:
#       print "You can't make a sandwich!"

#GOAL 2:  Modify that program to tell you: if you can make a sandwich, how many you can make

#bread = int(raw_input('What is your bread? '))
#jelly = int(raw_input('What is your jelly? '))
#pb = int(raw_input('What is your pb?' ))
#if bread == 2 and jelly == 1 and pb == 1:
#        print 'You can make 1 sandwich.'
#elif bread >= 2 and jelly >=1 and pb >=1:
#        print 'You can make', min(bread / 2, jelly / 1, pb / 1), 'sandwich(es).'
#else:
#        print "You can't make a sandwich!"

#GOAL 3:  Modify that program to allow you to make open-face sandwiches if you have an odd number of slices of bread ( )

#bread = int(raw_input('What is your bread? '))
#jelly = int(raw_input('What is your jelly? '))
#pb = int(raw_input('What is your pb?' ))
#if bread == 2 and jelly == 1 and pb == 1:
#        print 'You can make 1 sandwich.'
#elif bread >= 2 and bread%2==0 and jelly >=1 and pb >=1:
#        print 'You can make', min(bread / 2, jelly / 1, pb / 1), 'sandwich(es).'
#elif bread >= 2 and bread%2==1 and jelly >=1 and pb >=1:
#        print 'You can make', min(bread / 2, jelly / 1, pb / 1), 'sandwich(es), or you can make', min(bread, jelly, pb), 'fake sandwich(es).'
#else:
#        print "You can't make a sandwich!"

#bread = int(raw_input('What is your bread? '))
#jelly = int(raw_input('What is your jelly? '))
#pb = int(raw_input('What is your pb?' ))
#if bread >= 2 and jelly >=1 and pb >=1:
#        print 'You can make', min(bread / 2, jelly / 1, pb / 1), 'sandwich(es), or you can make', min(bread, jelly, pb), 'fake sandwich(es).'
#else:
#        print "You can't make a sandwich!"


#bread = int(raw_input('What is your bread? '))
#print 'bread/2 is ', bread/2
#print 'bread%2 is ', bread%2
#print '(bread/2)%2 is', bread/2%2

#GOAL 4:  Modify that program to tell you: if you're missing ingredients, which ones you need to be able to make your sandwiches

#bread = int(raw_input('What is your bread? '))
#jelly = int(raw_input('What is your jelly? '))
#pb = int(raw_input('What is your pb?' ))
#if bread >= 2 and jelly >=1 and pb >=1:
#        print 'You can make', min(bread / 2, jelly / 1, pb / 1), 'sandwich(es), or you can make', min(bread, jelly, pb), 'fake sandwich(es).'
#if bread <2:
#        print """You're missing bread!"""
#if jelly <1:
#        print """You're missing jelly!"""
#if pb<1:
#        print """You're missing pb!"""

#GOAL 5:  Modify that program to tell you: if you have enough bread and peanut butter but no jelly, that you can make a peanut butter sandwich.

#bread = int(raw_input('What is your bread? '))
#jelly = int(raw_input('What is your jelly? '))
#pb = int(raw_input('What is your pb? '))
#if bread >= 2 and jelly >=1 and pb >=1:
#        print 'You can make', min(bread / 2, jelly / 1, pb / 1), 'pbj sandwich(es), or you can make', min(bread, jelly, pb), 'fake pbj sandwich(es).'
#elif bread >= 2 and jelly==0 and pb >=1:
#        print 'You can make', min(bread / 2, pb / 1), 'pb sandwich(es), or you can make', min(bread, pb), 'fake pb sandwich(es).'
#if bread < 2:
#        print """You're missing bread!"""
#if pb < 1:
#        print """You're missing pb!"""
#if jelly < 1:
#        print """You're missing jelly!"""

#GOAL 6:  Or if you have more peanut butter and bread than jelly, that you can make a certain number of peanut butter & jelly sandwiches and a certain number of peanut butter sandwiches
#bread = int(raw_input('What is your bread? '))
#jelly = int(raw_input('What is your jelly? '))
#pb = int(raw_input('What is your pb? '))
#print "You have {0} bread, {1} jelly, and {2} pb".format(bread, jelly, pb)

#bread = int(raw_input('What is your bread? '))
#jelly = int(raw_input('What is your jelly? '))
#pb = int(raw_input('What is your pb? '))
#if bread >= 2 and jelly >=1 and pb >=1:
#    print 'You can make {0} sandwich(es).'.format(min(bread / 2, jelly / 1, pb / 1))
#    bread == 100000
#    print 'but now, magically, you can make {0} sandwich(es).'.format(min(bread/2)
#else:
#    print """You can't make a sandwich!"""
